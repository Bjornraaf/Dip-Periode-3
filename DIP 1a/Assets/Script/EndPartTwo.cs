using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EndPartTwo : MonoBehaviour
{
    // Start is called before the first frame update
    public void EndYourLife()
    {
        Application.Quit();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
